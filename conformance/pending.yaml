# Cases this port explicitly cannot handle yet.
# Rules (mirrored from the Ruby reference): entries need reason + ref;
# a pending case that passes raises the pending-satisfied alarm and
# MUST be unmarked.
---
iso.bundled_identifier.6823:
  reason: >-
    Ruby main rejects the printed date "2016-05-05-05" (a triple-dashed
    supplement date); one of the testsuite ledger's four known iso
    reference defects that must re-land as standalone implementation
    PRs.
  ref: tests/iso/_status.yaml known_mismatches
iso.corrigendum.6412:
  reason: >-
    The alias spelling "Amd 5:2010" renders with the parsed casing while
    the row human preserves the primary "AMD" spelling; Ruby main has
    the same divergence (known reference defect).
  ref: tests/iso/_status.yaml known_mismatches
iso.corrigendum.6509:
  reason: >-
    Same Amd/AMD alias-spelling divergence as iso.corrigendum.6412
    (known reference defect on Ruby main).
  ref: tests/iso/_status.yaml known_mismatches
nist.circular.0683:
  reason: >-
    The alias spelling "NBS CIRC 154supprev" renders as "154suprev"
    (supplement + revision) while the ledger canonical keeps the plain
    "154sup" supplement; Ruby main has the same divergence (known
    reference defect).
  ref: tests/nist/_status.yaml known_mismatches
nist.circular.0826:
  reason: >-
    The MR spelling "NBS.CIRC.supJun1925-Jun1926" renders without the
    publisher prefix the ledger canonical carries; Ruby main has the
    same divergence (known reference defect).
  ref: tests/nist/_status.yaml known_mismatches
nist.circular.0827:
  reason: >-
    Same MR CIRC supplement date-range divergence as nist.circular.0826
    (known reference defect on Ruby main).
  ref: tests/nist/_status.yaml known_mismatches
nist.circular.12111:
  reason: >-
    "NBS.CIRC" raises ParseFailed; Ruby main rejects it too
    ("Expected one of [ROOT]") — an unparsed ground-truth fixture.
  ref: tests/nist/_status.yaml known_mismatches
nist.circular.12114:
  reason: >-
    "NBS.CIRCe2" raises ParseFailed; Ruby main rejects it too — an
    unparsed ground-truth fixture.
  ref: tests/nist/_status.yaml known_mismatches
nist.handbook.12672:
  reason: >-
    The alias spelling "NIST HB 150-1-2017" renders number+edition
    "150-1e2017" while the ledger canonical collapses the compound
    number to "1e2017"; Ruby main has the same divergence (known
    reference defect).
  ref: tests/nist/_status.yaml known_mismatches
nist.handbook.12673:
  reason: >-
    Same HB trailing-year alias divergence as nist.handbook.12672 for
    "NIST HB 105-1-2019" and "NIST HB 150-1-2019" (known reference
    defect on Ruby main).
  ref: tests/nist/_status.yaml known_mismatches
nist.handbook.12681:
  reason: >-
    Same HB trailing-year alias divergence as nist.handbook.12672
    (known reference defect on Ruby main).
  ref: tests/nist/_status.yaml known_mismatches
nist.handbook.12682:
  reason: >-
    Same HB trailing-year alias divergence as nist.handbook.12672
    (known reference defect on Ruby main).
  ref: tests/nist/_status.yaml known_mismatches
nist.handbook.12716:
  reason: >-
    Same HB trailing-year alias divergence as nist.handbook.12672
    (known reference defect on Ruby main).
  ref: tests/nist/_status.yaml known_mismatches
nist.handbook.12717:
  reason: >-
    Same HB trailing-year alias divergence as nist.handbook.12672
    (known reference defect on Ruby main).
  ref: tests/nist/_status.yaml known_mismatches
nist.handbook.12718:
  reason: >-
    Same HB trailing-year alias divergence as nist.handbook.12672
    (known reference defect on Ruby main).
  ref: tests/nist/_status.yaml known_mismatches
nist.handbook.12719:
  reason: >-
    Same HB trailing-year alias divergence as nist.handbook.12672
    (known reference defect on Ruby main).
  ref: tests/nist/_status.yaml known_mismatches
nist.handbook.12720:
  reason: >-
    Same HB trailing-year alias divergence as nist.handbook.12672
    (known reference defect on Ruby main).
  ref: tests/nist/_status.yaml known_mismatches
nist.handbook.12721:
  reason: >-
    Same HB trailing-year alias divergence as nist.handbook.12672
    (known reference defect on Ruby main).
  ref: tests/nist/_status.yaml known_mismatches
nist.interagency_report.16251:
  reason: >-
    The bare "-upd" alias spelling renders with the dash prefix
    ("-upd1") while the ledger canonical carries the slash form
    ("/Upd1"); Ruby main has the same divergence (known reference
    defect).
  ref: tests/nist/_status.yaml known_mismatches
nist.interagency_report.16317:
  reason: >-
    Same bare "-upd" dash/slash divergence as
    nist.interagency_report.16251 (known reference defect on Ruby
    main).
  ref: tests/nist/_status.yaml known_mismatches
nist.interagency_report.16374:
  reason: >-
    Same bare "-upd" dash/slash divergence as
    nist.interagency_report.16251 (known reference defect on Ruby
    main).
  ref: tests/nist/_status.yaml known_mismatches
nist.special_publication.18068:
  reason: >-
    The bare "-upd" alias spelling renders "-upd1" while the ledger
    canonical carries "/Upd1"; Ruby main has the same divergence (known
    reference defect).
  ref: tests/nist/_status.yaml known_mismatches
nist.special_publication.18431:
  reason: >-
    The alias spelling "NIST SP 800-38a-add" renders "800-38a Add."
    while the ledger canonical preserves a different addendum spelling;
    Ruby main has the same divergence (known reference defect).
  ref: tests/nist/_status.yaml known_mismatches
nist.special_publication.19834:
  reason: >-
    "NIST.SP.955-S..uppl" raises ParseFailed; Ruby main rejects it too
    — an unparsed ground-truth fixture.
  ref: tests/nist/_status.yaml known_mismatches
nist.technical_note.12115:
  reason: >-
    "NBS.TN.467pt1.Add." raises ParseFailed; Ruby main rejects it too —
    an unparsed ground-truth fixture.
  ref: tests/nist/_status.yaml known_mismatches

# IEEE ledger known mismatches (tests/ieee/_status.yaml): the Ruby
# reference itself fails these rows today (verify loop output); this port
# reproduces the same divergences. Generated against pubid main.
# Remaining residual failures after these pends: stale _negative.yaml rows
# the current gem also parses (the corpus predates the gem fixes) plus a
# small documented TS gap set.
ieee.adopted_standard.8835:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.adopted_standard.8836:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.amendment.9349:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.corrigendum.5560:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.corrigendum.6457:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.corrigendum.6725:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.corrigendum.6846:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.corrigendum.8600:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.corrigendum.8811:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.corrigendum.8813:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.corrigendum.8838:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.csa_dual_published.6989:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.csa_dual_published.6991:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.iec_ieee_copublished.0829:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.iec_ieee_copublished.0831:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.iec_ieee_copublished.0838:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.iec_ieee_copublished.0840:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.iec_ieee_copublished.0841:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.iec_ieee_copublished.0856:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.iec_ieee_copublished.0857:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.iec_ieee_copublished.0859:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.iec_ieee_copublished.0860:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.iec_ieee_copublished.0872:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.iec_ieee_copublished.0877:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.iec_ieee_copublished.0882:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.iec_ieee_copublished.0887:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.iec_ieee_copublished.0888:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.iec_ieee_copublished.0890:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.iec_ieee_copublished.0891:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.iec_ieee_copublished.0893:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.iec_ieee_copublished.0895:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.iec_ieee_copublished.0896:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.iec_ieee_copublished.0897:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.iec_ieee_copublished.0899:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.iec_ieee_copublished.0905:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.iec_ieee_copublished.0911:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.iec_ieee_copublished.0922:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.iec_ieee_copublished.0923:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.joint_development.0915:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.joint_development.0919:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.joint_development.0941:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.joint_development.9361:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.joint_development.9365:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.joint_development.9378:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.nesc_edition.5711:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.nesc_handbook.5709:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.nesc_handbook.5710:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.project_draft_identifier.0846:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.project_draft_identifier.0885:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.project_draft_identifier.1286:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.project_draft_identifier.1428:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.project_draft_identifier.2017:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.project_draft_identifier.9277:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.project_draft_identifier.9278:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.project_draft_identifier.9297:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.project_draft_identifier.9298:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.project_draft_identifier.9316:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.project_draft_identifier.9331:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.project_draft_identifier.9332:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.project_draft_identifier.9337:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.project_draft_identifier.9343:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.project_draft_identifier.9351:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.0927:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.0928:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.0929:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.0930:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.0931:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.0932:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.0933:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.0934:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.0995:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.1021:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.1042:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.1089:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.1718:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.3942:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.4774:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.4775:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.4974:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.4980:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5026:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5061:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5087:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5174:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5179:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5350:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5352:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5378:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5379:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5392:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5406:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5463:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5464:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5506:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5518:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5559:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5571:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5576:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5599:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5623:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5624:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5654:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5661:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5720:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5728:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5752:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5753:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5769:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5777:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5806:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5814:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5815:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5818:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5855:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5857:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5885:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5890:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5933:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5937:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5941:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5957:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.5981:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6040:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6047:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6048:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6049:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6050:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6051:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6052:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6053:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6054:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6055:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6056:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6057:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6058:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6066:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6143:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6144:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6160:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6334:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6336:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6337:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6338:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6339:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6340:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6341:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6342:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6343:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6399:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6400:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6480:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6573:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6574:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6575:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6576:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6577:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6578:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6605:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6658:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6706:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6755:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6761:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6764:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6765:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6768:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6772:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6790:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6845:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6867:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6868:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6869:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6870:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6878:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6887:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6925:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6926:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.6979:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.7025:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.7026:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.7047:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.7108:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.7593:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8523:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8524:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8525:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8526:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8527:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8528:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8529:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8530:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8531:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8532:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8533:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8534:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8535:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8536:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8537:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8538:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8539:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8540:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8541:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8542:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8543:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8544:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8545:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8546:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8547:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8548:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8549:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8550:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8551:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8552:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8553:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8554:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8555:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8556:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8557:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8558:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8559:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8561:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8562:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8564:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8565:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8566:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8567:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8568:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8569:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8570:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8571:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8572:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8573:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8574:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8575:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8576:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8577:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8578:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8579:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8580:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8581:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8582:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8583:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8584:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8586:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8587:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8588:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8589:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8590:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8591:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8592:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8593:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8594:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8595:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8596:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8597:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8598:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8601:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8602:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8603:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8604:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8606:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8607:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8608:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8609:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8610:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8611:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8612:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8613:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8614:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8615:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8616:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8617:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8618:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8619:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8620:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8621:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8622:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8623:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8624:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8625:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8626:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8627:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8628:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8629:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8630:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8631:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8632:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8633:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8634:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8635:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8636:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8637:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8638:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8639:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8640:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8641:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8642:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8643:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8644:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8645:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8646:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8647:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8648:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8649:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8650:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8651:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8652:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8653:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8654:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8655:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8656:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8657:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8658:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8659:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8660:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8661:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8662:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8663:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8664:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8665:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8666:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8667:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8668:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8669:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8670:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8671:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8672:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8673:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8674:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8675:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8676:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8677:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8678:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8680:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8681:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8682:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8683:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8684:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8685:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8686:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8687:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8688:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8689:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8690:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8691:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8692:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8693:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8694:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8695:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8696:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8697:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8698:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8699:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8700:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8701:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8702:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8703:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8704:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8705:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8706:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8707:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8708:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8709:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8710:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8711:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8712:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8713:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8714:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8715:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8716:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8717:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8718:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8719:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8720:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8721:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8722:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8723:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8724:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8725:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8726:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8727:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8728:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8729:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8730:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8731:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8732:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8733:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8734:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8735:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8736:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8737:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8738:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8739:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8740:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8741:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8742:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8743:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8744:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8745:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8746:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8747:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8748:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8749:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8750:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8751:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8752:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8753:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8754:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8755:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8756:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8757:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8758:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8759:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8760:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8761:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8762:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8763:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8764:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8765:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8766:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8767:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8768:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8769:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8770:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8771:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8772:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8773:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8774:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8775:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8776:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8777:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8778:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8779:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8780:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8781:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8782:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8783:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8784:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8785:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8786:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8787:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8788:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8789:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8790:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8791:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8792:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8793:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8794:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8795:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8796:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8797:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8798:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8799:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8800:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8801:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8802:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8803:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8804:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8805:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8807:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8808:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8809:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8814:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8815:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8816:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8817:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8818:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8819:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8820:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8821:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8822:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8824:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8826:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8827:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8828:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8829:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8830:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8831:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8832:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8833:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8834:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8839:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8840:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8841:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8842:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8843:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8844:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8845:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8846:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8847:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8848:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8849:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8850:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8851:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8852:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8853:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8854:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8855:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8856:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8857:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8858:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8859:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8860:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8861:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8862:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8863:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8864:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8865:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8866:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8867:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8868:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8871:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8872:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8873:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8874:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8875:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8876:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8877:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8878:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8879:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8880:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8881:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8882:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8883:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8884:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8885:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8886:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8887:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8888:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8889:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8890:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8891:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8892:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8893:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8894:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8895:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8896:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8897:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8898:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8899:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8900:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8901:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8902:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8903:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8904:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8905:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8906:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8907:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8908:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8909:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8910:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8911:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8912:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8913:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8914:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8915:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8916:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8917:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8918:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8919:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8920:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8921:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8922:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8923:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8924:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8925:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8926:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8927:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8928:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8929:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8930:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8931:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8933:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8934:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8936:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8937:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8938:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8939:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8940:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8941:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8942:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8943:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8944:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8945:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8946:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8947:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.8948:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9037:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9122:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9124:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9267:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9268:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9269:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9270:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9274:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9284:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9286:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9290:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9292:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9294:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9295:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9303:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9304:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9306:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9309:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9313:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9314:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9317:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9318:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9341:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9345:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9347:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9348:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9350:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

cen_cenelec.fragment.0129:
  reason: >-
    "EN 60038/A1 FRAG2" raises ParseFailed; Ruby main rejects it too —
    the fragment grammar expects the "AMD1 FRAG2" spelling while the
    ledger canonical prints "/A1 FRAG2" (known reference defect).
  ref: tests/cen_cenelec/_status.yaml known_mismatches

astm.data_series.0051:
  reason: >-
    "ASTM DS55S-S1-EB" raises ParseFailed; Ruby main rejects it too —
    the data-series grammar reads subseries without the glued "S"
    suffix (known reference defect).
  ref: tests/astm/_status.yaml known_mismatches
astm.data_series.0052:
  reason: >-
    "ASTM DS55S-S10-EB" raises ParseFailed; Ruby main rejects it too
    (same glued-S data-series defect as astm.data_series.0051).
  ref: tests/astm/_status.yaml known_mismatches
astm.data_series.0053:
  reason: >-
    "ASTM DS55S-S4-EB" raises ParseFailed; Ruby main rejects it too
    (same glued-S data-series defect as astm.data_series.0051).
  ref: tests/astm/_status.yaml known_mismatches
