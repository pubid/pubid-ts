# Cases this port explicitly cannot handle yet.
# Rules (mirrored from the Ruby reference): entries need reason + ref;
# a pending case that passes raises the pending-satisfied alarm and
# MUST be unmarked (TODO.correct-test-suite/04).
---
# IEEE ledger known mismatches (tests/ieee/_status.yaml): the Ruby
# reference itself fails these rows today (verify loop output); this port
# reproduces the same divergences. Regenerated for the 8802 double-label
# corpus (gem #445): the ISO/IEC label keeps its dash-joined part; the
# residual below is the documented ledger divergence (alias families
# plus rows whose canonical human the reference itself cannot re-parse).
ieee.adopted_standard.8930:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.adopted_standard.8931:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.iec_ieee_copublished.0845:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.0896:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.0903:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.0910:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.0913:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.0915:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.0916:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.0926:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.0928:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.0930:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.0931:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.0932:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.0957:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.9141:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.9142:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.9143:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.9144:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.9145:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.9146:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.9147:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.9151:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.9259:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.9267:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.9293:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.project_draft_identifier.0902:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.project_draft_identifier.0906:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.project_draft_identifier.0912:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.project_draft_identifier.0922:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.project_draft_identifier.0927:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.project_draft_identifier.1294:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.project_draft_identifier.1432:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.project_draft_identifier.1898:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.project_draft_identifier.3171:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.project_draft_identifier.9328:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.project_draft_identifier.9331:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.project_draft_identifier.9333:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.project_draft_identifier.9336:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.project_draft_identifier.9383:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.standard.6934:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.standard.8721:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.standard.8848:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.standard.8850:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the 8802 double-label
    wave); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.neg.base.0029:
  reason: >-
    Documented TS negative gap (this port parses a line the reference rejects).
  ref: tests/ieee/_status.yaml known_mismatches
ieee.neg.standard.0196:
  reason: >-
    Documented TS negative gap (this port parses a line the reference rejects).
  ref: tests/ieee/_status.yaml known_mismatches
