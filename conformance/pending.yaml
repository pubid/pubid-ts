# Cases this port explicitly cannot handle yet.
# Rules (mirrored from the Ruby reference): entries need reason + ref;
# a pending case that passes raises the pending-satisfied alarm and

# IEEE ledger known mismatches (tests/ieee/_status.yaml): the Ruby
# reference itself fails these rows today (verify loop output); this port
# reproduces the same divergences. Generated against pubid main.
# Remaining residual failures after these pends: stale _negative.yaml rows
# the current gem also parses (the corpus predates the gem fixes) plus a
# small documented TS gap set.
# ashrae ledger: gem-known mismatches reproduced one-for-one (verify loop /tmp/verify-ashrae.rb).

# bsi ledger: gem-known mismatches reproduced one-for-one (verify loop /tmp/verify-bsi.rb).

# IEEE residual (pubid-testsuite #20, gem pubid#425): the reference
# documents spell these rows inconsistently with their own canonical
# forms - the printed-form alias family the Ruby reference keeps as a
# documented known-mismatch ledger (tests/ieee/_status.yaml, 22 rows);
# this port reproduces the same divergences. Plus the two long-known
# TS negative gaps (neg.base.0029, neg.standard.0196).
ieee.adopted_standard.9492:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.adopted_standard.9493:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.iec_ieee_copublished.0841:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.joint_development.0922:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.joint_development.0952:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.joint_development.9904:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.joint_development.9910:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.joint_development.9929:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.neg.base.0029:
  reason: >-
    Documented TS negative gap (this port parses a line the reference rejects).
  ref: tests/ieee/_status.yaml known_mismatches
ieee.neg.standard.0196:
  reason: >-
    Documented TS negative gap (this port parses a line the reference rejects).
  ref: tests/ieee/_status.yaml known_mismatches
ieee.project_draft_identifier.0847:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.project_draft_identifier.0884:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.project_draft_identifier.0929:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.project_draft_identifier.1293:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.project_draft_identifier.1435:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.project_draft_identifier.1915:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.project_draft_identifier.3200:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.project_draft_identifier.9765:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.project_draft_identifier.9768:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.project_draft_identifier.9835:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.7090:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9279:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9410:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.standard.9412:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's own verify loop reports a known mismatch); this port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
