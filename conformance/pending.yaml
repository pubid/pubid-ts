# Cases this port explicitly cannot handle yet.
# Rules (mirrored from the Ruby reference): entries need reason + ref;
# a pending case that passes raises the pending-satisfied alarm and

# IEEE ledger known mismatches (tests/ieee/_status.yaml): the Ruby
# reference itself fails these rows today (verify loop output); this port
# reproduces the same divergences. Generated against pubid main.
# Remaining residual failures after these pends: stale _negative.yaml rows
# the current gem also parses (the corpus predates the gem fixes) plus a
# small documented TS gap set.
# ashrae ledger: gem-known mismatches reproduced one-for-one (verify loop /tmp/verify-ashrae.rb).

# bsi ledger: gem-known mismatches reproduced one-for-one (verify loop /tmp/verify-bsi.rb).

# IEEE residual (pubid-testsuite #20, gem pubid#425): the reference
# documents spell these rows inconsistently with their own canonical
# forms - the printed-form alias family the Ruby reference keeps as a
# documented known-mismatch ledger (tests/ieee/_status.yaml, 22 rows);
# this port reproduces the same divergences. Plus the two long-known
# TS negative gaps (neg.base.0029, neg.standard.0196).
# IEEE ledger known mismatches (tests/ieee/_status.yaml): the Ruby
# reference itself fails these rows today (verify loop output); this port
# reproduces the same divergences. Regenerated for the stage-draft corpus
# (the D= notation, the compound both-systems form, P-state identity and
# the IEC/IEEE URN): the previously pended P-suppression rows now pass and
# the residual set below is the documented alias-family divergence.
ieee.adopted_standard.9514:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.adopted_standard.9515:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.iec_ieee_copublished.0842:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.0906:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.0912:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.0918:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.0919:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.0921:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches
ieee.joint_development.0922:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.0932:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.0933:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.0935:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.0936:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.0937:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.0973:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.9858:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.9864:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.joint_development.9884:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.project_draft_identifier.0915:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.project_draft_identifier.0928:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.project_draft_identifier.1313:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.project_draft_identifier.1455:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.project_draft_identifier.1935:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.project_draft_identifier.3222:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.project_draft_identifier.9918:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.project_draft_identifier.9919:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.project_draft_identifier.9966:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.standard.7112:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.standard.9301:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.standard.9432:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.standard.9434:
  reason: >-
    Ruby main diverges from the ledger canonical on this row (the testsuite's
    own verify loop reports a known mismatch after the stage-draft wave); this
    port reproduces the gem's behavior.
  ref: tests/ieee/_status.yaml known_mismatches

ieee.neg.base.0029:
  reason: >-
    Documented TS negative gap (this port parses a line the reference rejects).
  ref: tests/ieee/_status.yaml known_mismatches
ieee.neg.standard.0196:
  reason: >-
    Documented TS negative gap (this port parses a line the reference rejects).
  ref: tests/ieee/_status.yaml known_mismatches
